import { ComponentMeta } from '../component/meta';
export declare function resolveImportRelativePath(coreName: string): string;
export declare function getPartName(part: string, name: string): string;
export default function genLibEntryTemplate(componentsMeta: ComponentMeta[]): string;
