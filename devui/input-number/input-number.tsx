import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-input-number',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-input-number</div>
    }
  }
})