import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-quadrant-diagram',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-quadrant-diagram</div>
    }
  }
})