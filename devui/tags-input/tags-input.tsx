import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tags-input',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tags-input</div>
    }
  }
})