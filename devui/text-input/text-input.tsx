import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-text-input',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-text-input</div>
    }
  }
})