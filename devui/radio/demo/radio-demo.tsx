import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-radio-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-radio-demo</div>
    }
  }
})