import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-checkbox',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-checkbox</div>
    }
  }
})