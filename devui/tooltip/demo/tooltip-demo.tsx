import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tooltip-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tooltip-demo</div>
    }
  }
})