import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-modal-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-modal-demo</div>
    }
  }
})