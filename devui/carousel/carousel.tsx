import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-carousel',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-carousel</div>
    }
  }
})