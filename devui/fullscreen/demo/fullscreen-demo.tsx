import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-fullscreen-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-fullscreen-demo</div>
    }
  }
})