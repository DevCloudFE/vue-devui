import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-time-axis-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-time-axis-demo</div>
    }
  }
})