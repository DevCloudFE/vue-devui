import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tree-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tree-demo</div>
    }
  }
})