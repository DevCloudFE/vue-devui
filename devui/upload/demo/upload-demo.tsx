import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-upload-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-upload-demo</div>
    }
  }
})