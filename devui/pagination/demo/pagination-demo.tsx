import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-pagination-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-pagination-demo</div>
    }
  }
})