import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-pagination',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-pagination</div>
    }
  }
})