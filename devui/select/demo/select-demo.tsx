import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-select-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-select-demo</div>
    }
  }
})