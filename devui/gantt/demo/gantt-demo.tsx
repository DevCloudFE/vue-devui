import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-gantt-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-gantt-demo</div>
    }
  }
})