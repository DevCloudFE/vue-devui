import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-cascader',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-cascader</div>
    }
  }
})