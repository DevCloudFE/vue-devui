import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-cascader-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-cascader-demo</div>
    }
  }
})