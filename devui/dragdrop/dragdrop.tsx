import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-dragdrop',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-dragdrop</div>
    }
  }
})