import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-rate-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-rate-demo</div>
    }
  }
})