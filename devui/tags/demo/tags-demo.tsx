import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tags-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tags-demo</div>
    }
  }
})