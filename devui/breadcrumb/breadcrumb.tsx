import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-breadcrumb',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-breadcrumb</div>
    }
  }
})