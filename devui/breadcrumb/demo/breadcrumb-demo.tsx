import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-breadcrumb-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-breadcrumb-demo</div>
    }
  }
})