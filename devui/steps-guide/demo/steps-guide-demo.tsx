import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-steps-guide-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-steps-guide-demo</div>
    }
  }
})