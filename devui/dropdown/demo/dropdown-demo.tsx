import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-dropdown-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-dropdown-demo</div>
    }
  }
})