import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-dropdown',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-dropdown</div>
    }
  }
})