import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-transfer',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-transfer</div>
    }
  }
})