import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-transfer-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-transfer-demo</div>
    }
  }
})