import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-splitter-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-splitter-demo</div>
    }
  }
})