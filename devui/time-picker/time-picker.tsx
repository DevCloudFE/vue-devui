import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-time-picker',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-time-picker</div>
    }
  }
})