import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tree-select-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tree-select-demo</div>
    }
  }
})