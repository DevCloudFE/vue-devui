import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tree-select',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tree-select</div>
    }
  }
})