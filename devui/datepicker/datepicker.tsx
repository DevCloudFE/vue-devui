import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-datepicker',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-datepicker</div>
    }
  }
})