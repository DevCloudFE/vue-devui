import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-popover-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-popover-demo</div>
    }
  }
})