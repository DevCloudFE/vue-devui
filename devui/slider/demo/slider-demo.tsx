import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-slider-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-slider-demo</div>
    }
  }
})