import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-data-table',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-data-table</div>
    }
  }
})