import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-editable-select-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-editable-select-demo</div>
    }
  }
})