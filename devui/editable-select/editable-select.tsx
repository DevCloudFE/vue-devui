import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-editable-select',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-editable-select</div>
    }
  }
})