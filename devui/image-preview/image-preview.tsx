import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-image-preview',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-image-preview</div>
    }
  }
})