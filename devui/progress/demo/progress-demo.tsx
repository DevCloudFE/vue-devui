import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-progress-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-progress-demo</div>
    }
  }
})