import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-sticky-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-sticky-demo</div>
    }
  }
})