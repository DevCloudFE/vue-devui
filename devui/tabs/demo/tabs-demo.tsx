import { defineComponent } from 'vue'

export default defineComponent({
  name: 'd-tabs-demo',
  props: {
  },
  setup(props, ctx) {
    return () => {
      return <div>devui-tabs-demo</div>
    }
  }
})